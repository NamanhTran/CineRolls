import { Container } from '@material-ui/core';
import React from 'react';
import Search from './Search';

const App = () => {
    return (
        <Container > 
            <Search />
        </Container>
    );
};

export default App;